# Implementation
Implementation of our DPSS protocol

To use pointproofs, first clone `opDPSSTeam/pointproofs` and run `make build` to generate `target` files.
Copy the `target` folder to `pkg` and rename it as `pointproofs_target`.
